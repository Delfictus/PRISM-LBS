# Meta Merkle Ledger

This directory stores Merkle proofs and flag manifests emitted by the MEC feature registry. Each file name follows:

```
meta_flags_<timestamp>_<root>.json
```

Artifacts are immutable and governed by the determinism manifest.
